absl-py
attrs>=19.1.0
flatbuffers>=2.0
jax
jaxlib
matplotlib
numpy  # with current versions of opencv-contrib-python and/or other dependencies here, numpy no longer needs to be pinnned to be <2 here, so that users of the generated python package can use new version 2.x versions of numpy (our setup.py takes is package requirements from the current file, hence this effect).
opencv-contrib-python
protobuf>=4.25.3,<5
sounddevice>=0.4.4
sentencepiece