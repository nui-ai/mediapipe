# OpenCV configuration for CPU-only builds on Linux

def opencv_configure():
    """Configure OpenCV for CPU-only image processing."""
    pass  # basic configuration, no special setup needed for CPU-only builds